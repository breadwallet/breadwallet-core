#
# secp256k1 'install
#
cd ./vendor
git clone https://github.com/bitcoin-core/secp256k1.git secp256k1
cd secp256k1
git checkout e34ceb3
rm -rf .git
cd ..
# If needed:
#   Xcode: header path with '../vendor'
#   BRKey: #include path with '../vendor/

