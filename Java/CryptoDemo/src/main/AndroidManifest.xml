<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.breadwallet.cryptodemo">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CoreCryptoApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".TransferCreateSendActivity"
            android:label="Create Transfer"/>
        <activity
            android:name=".TransferCreateSweepActivity"
            android:label="Create Sweep Transfer"/>
        <activity
            android:name=".TransferCreatePaymentActivity"
            android:label="Create Payment Transfer"/>
        <activity
            android:name=".TransferDetailsActivity"
            android:label="Transfer Details" />
        <activity
            android:name=".TransferListActivity"
            android:label="Transfers" />
        <activity
            android:name=".WalletListActivity"
            android:label="Wallets">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>